import { Route } from "@angular/router";
import { ValidateComponent } from "./components/validate/validate.component";

export const validateRoutes: Route[] = [
    {
        path:'',
        component:ValidateComponent,
    }
]